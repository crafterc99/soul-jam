import Phaser from 'phaser';

export class AnimationLoader {
  static load(scene: Phaser.Scene): void {
    // Placeholder - will be filled when sprite assets are provided
    // For now, we use colored rectangles via graphics
  }
}
