{
  "name": "soul-jam",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "npx serve dist -s -l tcp://0.0.0.0:${PORT:-3000}",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "serve": "^14.2.6"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "playwright": "^1.58.2",
    "typescript": "^5.3.3",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
